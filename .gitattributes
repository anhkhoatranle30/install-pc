# Script chạy trong Linux/WSL PHẢI có line ending LF.
# Nếu git checkout ra CRLF, bash báo: bad interpreter: /bin/bash^M
*.sh text eol=lf

# Script Windows giữ CRLF
*.ps1 text eol=crlf
*.cmd text eol=crlf

# Con trỏ chuột là file nhị phân, không để git đụng line ending.
*.cur binary
